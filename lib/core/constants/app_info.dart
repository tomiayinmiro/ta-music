// TODO(feedback): replace with the real installed version via
// `package_info_plus` once that dependency is added — hardcoded for now, see
// CLAUDE.md's Feedback & Help decisions.
const String kAppVersion = '1.0.0';
